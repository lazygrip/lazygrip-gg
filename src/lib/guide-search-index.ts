// GENERATED FILE -- do not hand-edit. Produced by scripts/generate-guide-search-index.js
// from the site's own build output, so it reflects exactly what's rendered on the page.
// After changing any guide page's content:
// npm run build && node scripts/generate-guide-search-index.js
// then commit this file alongside the guide change.

export type GuideSearchEntry = {
  page: string
  pageTitle: string
  section: string
  href: string
  body: string
}

export const GUIDE_SEARCH_INDEX: GuideSearchEntry[] = [
  {
    "page": "/guide",
    "pageTitle": "Overview",
    "section": "Before you open the editor",
    "href": "/guide#before-you-open-the-editor",
    "body": "The single biggest mistake new users make is opening GRIP-EMS and typing spell names before they know what their sequence is supposed to do. The addon executes a rotation you have already designed, it does not design it for you. Sitting down with no plan produces a sequence that technically runs but delivers mediocre results because the step ordering and spacing came from guessing rather than understanding your spec. Before you build anything, spend twenty minutes with the Icy Veins guide or your spec Discord and answer four questions. What is your highest priority spell and how often does it need to fire? What is the cooldown you cannot afford to miss on pull? Which spells can be skipped when unavailable without breaking the rotation, and which ones cannot be skipped? And do you need the sequence to behave differently in different content, such as a different opener for Mythic+ versus raid? The answers to those questions determine your step count, your step function choice, your reset conditions, and whether you need context versions. The Building sequences section walks through exactly how those answers translate into a working sequence for any spec."
  },
  {
    "page": "/guide",
    "pageTitle": "Overview",
    "section": "About the sequences on this site",
    "href": "/guide#about-the-sequences-on-this-site",
    "body": "Every sequence published on LazyGrip includes the content type it was validated at, the key level or difficulty tier, and the talent string it was built for. If a sequence does not list validation data, treat it as a starting point to adapt rather than a finished product to import and run blind. A sequence validated at plus 10 on one hero talent path will produce different numbers on a different path because the spells and their interactions differ. The talent string ships with every sequence for exactly this reason. If your talents do not match, the sequence is still worth importing as a structural reference, but plan on a validation pass before you rely on it in serious content. The GRIP-EMS Guide Start with Installation"
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "Step 1: Download and enable",
    "href": "/guide/installation#step-1-download-and-enable",
    "body": "1 Download from CurseForge, Wago, or WoWInterface GRIP-EMS is free on all three platforms. Use whichever addon manager you already have. CurseForge Wago WoWInterface 2 Enable the addon in-game At the character select screen, click AddOns in the bottom left corner and make sure GRIP-EMS is checked. Log into your character. If you do not see GRIP-EMS in the list, your addon manager did not install it correctly. Try a manual download and drop the folder into your World of Warcraft\\_retail_\\Interface\\AddOns directory."
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "Step 2: The settings you should still check",
    "href": "/guide/installation#step-2-the-settings-you-should-still-check",
    "body": "As of GRIP-EMS v2.3.14, the addon forces Key Down Casting on automatically every time you log in, so that part of setup is no longer something you need to touch. Two things still matter for sequences to feel right, and neither is set to an ideal value by default. 1 Key Down Casting is handled for you now GRIP-EMS fires through WoW's key-down event system. By default WoW uses key-up, which means a sequence would register the press only when you release the key rather than when you press it. At 150ms intervals that is the difference between a functioning rotation and nothing happening at all. Starting in v2.3.14, GRIP-EMS sets ActionButtonUseKeyDown to enabled itself on every login, whether or not you have ever opened the settings. It is no longer a row in the CVar Health tab and there is nothing to fix here manually. Running an older version than 2.3.14? Update through your addon manager first. On anything older, open /gems settings , go to the CVar Health tab, and click Fix if the row is not green. That is the single most common reason a pre-2.3.14 install does nothing on keypress. 2 Verify your SpellQueueWindow The SpellQueueWindow controls how many milliseconds before a GCD ends WoW will accept your next cast input. The CVar Health tab shows your current value and flags it if it looks off. The default of 400ms is fine to leave alone for now. The full explanation of what SQW does, why it matters, and how to tune it for your connection is on the Settings page. 3 Set your click rate GRIP-EMS advances one step per keypress. Your hardware or software needs to send repeated keypresses at a consistent interval. The right value depends on your setup but 150ms is a reliable starting point that works across most hardware and latency combinations. If you are using Razer hardware, set your repeat rate to 150ms in Synapse. Once you have a sequence running and some combat data logged, the Tempo Advisor on the Settings page shows a recommended click rate worked out from your sequence's actual step timing and how you play it. That number is more accurate than any static figure and gets sharper the more you play that sequence."
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "What you will not see and why",
    "href": "/guide/installation#what-you-will-not-see-and-why",
    "body": "GRIP-EMS does not work like the legacy program, which creates a draggable button you place on a bar and bind. GRIP-EMS binds a key directly to a sequence inside the addon. For sequences with multiple versions, a macro is created and can be placed on your action bar, but for single-version sequences there is nothing to drag. If you are looking for a button to appear and it is not appearing, that is expected behavior and not a bug. To bind a key to a sequence, open the sequence in the GRIP-EMS editor, go to the Keybinds tab, and assign a key there. The bind is stored per-spec, so switching specs gives you a clean slate for a different rotation on the same key. If you specifically want a button on your action bar that you can drag and drop the way you would with the legacy program, that workflow still exists as an optional add-on rather than a built-in GRIP-EMS feature. Daxomault's GRIP-EMS Actionbutton Override plugin adds action bar override and drag-and-drop keybinding on top of GRIP-EMS. It is not required and the keybind-only approach above is still the default and the one most players end up using, but it is there if you prefer to keep sequences on your bars."
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "Quick sanity check",
    "href": "/guide/installation#quick-sanity-check",
    "body": "Before moving on, confirm these four things are true: ✓ On v2.3.14 or later, nothing needed; on older versions, Cvar Health tab shows green with no Fix button visible ✓ You have at least one sequence imported or created ✓ That sequence has a keybind assigned in the Keybinds tab ✓ You are in Bear Form or your spec's required form when testing"
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "Troubleshooting common problems",
    "href": "/guide/installation#troubleshooting-common-problems",
    "body": "Keybind is set but nothing fires First, update GRIP-EMS through your addon manager. As of v2.3.14, Key Down Casting is forced on automatically and this is no longer something you can misconfigure. If you are still on an older version, run /gems settings, go to the Cvar Health tab, and click Fix if the row is not green; this solves the majority of pre-2.3.14 keybind problems. On 2.3.14 or later, if the sequence still does not fire, check that you have a target selected and that the sequence has a keybind assigned in the Keybinds tab inside the editor, not just in WoW's default keybind menu. Run /gems keys to check whether another addon or click binding is quietly eating the key before GRIP-EMS ever sees it. Key still shows correctly in /gems but pressing it does nothing after a loading screen This was a known bug through v2.4.0: GRIP-EMS re-applies sequence keybinds at runtime rather than parking them in WoW's own binding list, so a loading screen could silently drop the live binding while /gems still listed it as bound. Fixed in v2.4.1. Update GRIP-EMS through your addon manager. If you are already current and still see this, it is not the same bug; run /gems keys to check for a genuine conflict instead. I put a sequence's macro on my action bar and pressing it does nothing Multi-version sequences generate a macro you can place on a bar, but clicking that macro with your mouse or pressing it from an action bar slot is not the same as a Keybinds-tab bind, and it will not fire the sequence correctly on its own. GRIP-EMS is built around binding a key directly to the sequence inside the editor's Keybinds tab, not around action bar buttons. If you want the drag-and-drop, click-to-fire workflow you may be used to from the legacy program, install Daxomault's GRIP-EMS Actionbutton Override addon, which adds that behavior back as an option. Sequence fires once then stops GRIP-EMS requires continuous repeated keypresses, not a single press and hold. Your hardware or software needs to be configured to send repeated keypress events. At 150ms that is roughly 6 to 7 presses per second. A single press fires one step and stops. Sequence shows greyed out after a /reload in arena or M+ This was a known bug where the addon waited on a match flag that never cleared mid-fight, leaving sequences inactive until the match ended. Update GRIP-EMS through your addon manager. Clicking a greyed-out sequence row in the editor also re-activates it on the spot. Sequence key stops working randomly while grinding, mount and dismount fixes it This was a bug where the addon waited on a match-privacy flag that never cleared outside of arena or Mythic+ specifically, which could leave a sequence marked dormant longer than it should have been in some overland situations. Update GRIP-EMS through your addon manager. Clicking the greyed-out sequence row in the editor re-activates it immediately without needing to relog. Sequence key dies after skyriding or leaving a vehicle The keybind suspend-and-restore runs inside the secure engine and survives skyriding, vehicle exits, and possession. If you are hitting this, update GRIP-EMS through your addon manager. You can also press your sequence key while skyriding over an attackable target and it will dismount and fire step 1 in a single press. Sequence key fires mid-air or vehicle keys are dead on first flight of the session This was a real first-takeoff bug: ground binds could stay live mid-air, or vehicle keys never woke up at all, on the first flight of a session specifically. It is fixed. An out-of-combat watchdog now heals the swap the moment you take off and restores it on landing, so the first flight of a session behaves the same as every other one. If you are current and still hitting this, it is not the same bug; report it in the Discord with your GRIP-EMS version and whether it happens on every login or only occasionally. Another addon keeps stealing my sequence keybinds Some addons override-bind the same keys GRIP-EMS uses, and whoever loads last wins. Run /gems keys, or click Check Conflicts on the Keybind tab, and GRIP-EMS checks every key your sequences use, including vehicle and pet battle slots, against your saved bindings and names the addon behind a conflicting binding where it can. It clears two known kinds of conflict for you directly; Undo backs out the last change if something else grabs the key again afterward. GRIP-EMS does not fight for the bind otherwise, it just tells you who took it. Sequence says a spell cannot be found, but it works fine once I am in the right form or proc This is expected behavior, not a bug. GRIP-EMS checks whether a spell is castable right now before it will queue it in a step. Some spells only exist, or only become the correct version, in a specific state, such as Void Volley replacing Void Bolt only while a Shadow Priest is in Voidform, or a spec-specific proc swapping what a button actually casts. Outside that state the addon correctly reports it cannot find the spell, then the step behaves normally the moment you are actually in the state where the spell exists. If the step is working in practice once the condition is met, there is nothing to fix. If you want the warning to stop appearing while testing outside the triggering state, an addon like ErrorMonster can redirect the message so it does not clutter your chat window."
  },
  {
    "page": "/guide/installation",
    "pageTitle": "Installation",
    "section": "Commands worth knowing",
    "href": "/guide/installation#commands-worth-knowing",
    "body": "/gems Opens the GRIP-EMS sequence editor /gems settings Opens settings including Cvar Health /gems binds Shows all currently bound sequences for your active spec /gems keys Checks every sequence, vehicle, and pet battle key against your saved bindings and names conflicts /gems debug on Enables debug output to chat, useful when something is not firing /gems debugwindow Opens the debug window with additional diagnostic information /gems validate Runs spell validation across all your sequences and reports stale spells /gems revalidate Forces a full revalidation, useful after a patch or respec Next: Settings →"
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Key Down Casting",
    "href": "/guide/settings#key-down-casting",
    "body": "Automatic as of v2.3.14 This is the WoW setting ActionButtonUseKeyDown . It controls whether your abilities fire when you press a key or when you release it. GRIP-EMS fires on the press, and it needs this set to on to work correctly. Leave it off, and your sequence either does not advance at all or advances on the wrong event, with an extra 50 to 100ms of dead time on every single press, eating into your rotation. As of GRIP-EMS v2.3.14, this is no longer a preference or a manual step. The addon forces it on itself every time you log in, whether or not you have ever opened the settings. It has been removed from the CVar Health tab entirely since it is required for the engine to run, not something you weigh up. Run /gems status any time to confirm the live value. On a version older than 2.3.14, you will still need to fix this yourself. Run /gems settings, go to the CVar Health tab, and click Fix if the row is not green. Do it outside of combat; WoW locks some CVars while you are in a fight, which is why the Fix buttons grey out mid-pull. The Installation page covers this in more detail for older versions."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Hold While Channeling",
    "href": "/guide/settings#hold-while-channeling",
    "body": "Added in v2.4.0 · Works on any channel as of v2.4.14 · Set per sequence version This setting decides what happens when your keypress lands in the middle of a channel. Without it, a press mid-channel casts nothing and still burns a step, which is the same dead-step problem covered elsewhere on this page, just triggered by a channel instead of a cooldown. With Hold cursor on, a press landing mid-channel casts nothing and leaves the step counter where it was, so the channel finishes and the next real press picks up from the same step instead of one further along. There is a third option, Hold + release at max, that ends an empowered spell the moment it reaches maximum rank rather than wasting time sitting at max charge while you keep pressing. Through v2.4.13, the entire Hold While Channeling control only appeared in the dropdown if your spellbook contained an actual empowered spell, in practice meaning Evoker, even though the underlying hold mechanism has always worked correctly on any channeled spell for any class. That gating was a UI bug, not a design decision, and it is fixed in v2.4.14: the setting now shows for everyone. If you play a spec with a channel and no empowered spell, such as Shadow Priest's Mind Flay or Void Torrent, you now have this option where you did not before. This matters beyond convenience. As of the v2.4.14 release, using Hold While Channeling properly is the addon author's own stated replacement for hand-writing [channeling] and [nochanneling] guards into your step conditionals: fewer characters spent on conditionals per step, and more consistent behavior than chaining bracket logic by hand. It does not remove the need for [combat] , which still matters for the reasons covered on the How it works page. If you are sequencing a charged or empowered spell like Fire Breath or Upheaval, Hold While Channeling, and specifically Hold + release at max, is now the first thing to reach for rather than the guard-step or separate-key workarounds on the Building sequences page. Those workarounds predate this fix and existed specifically to route around the automated-cancel hitch that Hold + release at max is built to solve directly. Try the setting first; fall back to the guard-step or separate-key approach only if it does not behave the way you need for your spec."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Spell Queue Window",
    "href": "/guide/settings#spell-queue-window",
    "body": "Settings > CVar Health > Macro Sequencing The WoW setting SpellQueueWindow . This controls how many milliseconds before a GCD ends WoW will accept your next cast input. Blizzard's default is 400ms, which is also the ceiling. WoW calls this Custom Lag Tolerance under Options > Network. GRIP-EMS surfaces it in CVar Health so you do not have to go hunting for it. The way it works: say your GCD is 1.5 seconds. With SQW at 400ms, the queue window opens at 1100ms into that GCD. Any press inside that window gets queued. The last press before the GCD ends is the one that fires, replacing anything queued before it. Example: GCD is 1.5s, SQW is 400ms. Queue window opens at 1100ms. You press at 1200ms and again at 1450ms. The 1450ms press is what fires. The trap most people fall into: setting SQW lower than your latency. If your ping is 120ms and you force SQW down to 50ms, your queued press can reach the server too late to register, which means dead GCDs and a rotation that feels like it skips. SQW must always be higher than your latency. If you are unsure, leave the Dynamic SQW Optimiser on and let it handle the math for you."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Dynamic SQW Optimiser",
    "href": "/guide/settings#dynamic-sqw-optimiser",
    "body": "Settings > CVar Health This is the setting that does the SQW math for you and keeps it updated as your connection moves around during a session. It continuously monitors your world latency, using a smoothed average sampled every 10 seconds, ignoring one-off spikes that would otherwise throw your numbers off. From that, it sets SQW to your current latency plus a configurable Safety Buffer, kept inside the 50 to 400ms ceiling. So if your latency is 120ms and your Safety Buffer is 50ms, SQW lands at around 170ms. Your latency jumps to 160ms mid-key, SQW adjusts. Safety Buffer Yours to set between 50 and 200ms. Higher buffer means fewer dropped presses, lower feels snappier. 100ms is a sensible middle ground for most players. If your connection is stable and low-latency, push it lower. If it is inconsistent, go higher. While the Optimiser is running, it has exclusive control over the SQW value. You will see \"Managed by SQW Optimiser\" in the manual control and the slider locks. If you want to set SQW by hand, turn the Optimiser off first. The SQW Optimiser tunes one thing: the SpellQueueWindow CVar, based on your latency. It does not look at your sequence, your spells, or your click rate. For a recommendation on how fast you should actually be pressing your keybind, that is the Tempo Advisor below, a separate system entirely."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Finding your way around CVar Health",
    "href": "/guide/settings#finding-your-way-around-cvar-health",
    "body": "Settings > CVar Health As of v2.3.13, the CVar Health dashboard covers 255 game variables across 14 sections, up from 212 in earlier versions. New sections cover Combat Audio, nameplate content toggles, accessibility motion and vision settings, raid graphics mirrors, and dragonriding comfort options, alongside the SQW and Key Down Casting settings this page already covers in detail. With that many CVars, finding a specific one by scrolling is no longer realistic, so the tab got a few navigation aids. A search box lets you type part of a CVar's name and jump straight to it. A show-only-issues filter hides every row that is already correctly set, so you see just what needs attention. Section headers are colour-coded, meaning a section containing a misconfigured row shows red before you even expand it, so you know where to look without opening every section in turn. Numeric CVars, fifty of them, are sliders rather than typed fields, and on/off CVars are tickboxes. The graphics dropdowns pull their tier names directly from the game client, so options like shadow quality, SSAO, and liquid detail match the wording in WoW's own System menu instead of using GRIP-EMS's own labels. maxFPS now goes up to 144. None of this changes what any individual CVar does or how you should set it, including SQW and Key Down Casting above. It only changes how you find and adjust them. If you learned the old dashboard layout before v2.3.13, the values and recommendations on this page still apply; only the search, filtering, and slider presentation are new."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "The Delve CVar profile",
    "href": "/guide/settings#the-delve-cvar-profile",
    "body": "Automatic as of v2.3.12 GRIP-EMS ships with built-in CVar profiles that swap automatically based on the content you are in, and as of v2.3.12 Delves get their own dedicated profile rather than running on the same baseline as general open-world content. Before this, a Delve run used your General graphics profile even though the addon already knew you were inside a Delve, which meant nameplate range and soft-targeting were tuned for the wrong context. The built-in Delve profile turns on enemy minion nameplates, extends nameplate range to 41 yards, and widens the soft-target arc, all automatically the moment you enter a Delve, at any tier, with no setup required on your part. If you had already mapped your own custom profile onto Delves before v2.3.12, your mapping still takes priority. The rule is that a manual mapping on a tier key always beats the built-in default. If you want the new built-in behavior instead of your old custom mapping, go to the Profile Map Editor and remap the DelvesLow and DelvesHigh tier keys back to the built-in profile."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Click Rate",
    "href": "/guide/settings#click-rate",
    "body": "Settings > General How fast you are pressing your sequence key, in milliseconds. GRIP-EMS advances one step per keypress and does not cap how fast you press, so this number is really a reference point that also controls the timing of any imported pauses in a sequence. The default is 250ms, and the slider will not go below 100ms. The reason it floors at 100ms is that nobody is realistically pressing a key faster than ten times a second, and at that speed, you would be running past steps before the spells they contain can land. GRIP-EMS calls 100ms the human and hardware floor. Per-Character Click Rate , under the same menu, is a setting scoped to your current character only and overrides the global value while that character is active. Range is 0 to 1000ms in steps of 10. Set it to 0 to fall back to the shared global value. This is also the only way to go below the 100ms floor if you have an edge case that needs it, down to 10ms. GRIP-EMS will pop a warning on screen when you drop under 100. Pressing faster than your spells can land does not make your rotation faster. The GCD is the real speed limit, around 1.5 seconds for most specs and shorter with haste. If you are pressing at 80ms and your GCD is 1.4 seconds, you are firing roughly 17 keypresses per GCD and advancing the sequence 17 steps before a single spell lands. That is not a faster rotation; it is a broken one. The sweet spot is pressing at roughly your cast pace, and the Tempo Advisor below is the most personalized way to find that number for your character in real time."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Keybind Conflicts",
    "href": "/guide/settings#keybind-conflicts",
    "body": "Added in v2.4.0 · /gems keys or the Keybind tab Run /gems keys , or click Check Conflicts on the Keybind tab, and GRIP-EMS checks every key your sequences use, plus vehicle and pet battle slots, against your saved bindings: what the keypress actually reaches, cast-redirect modifiers, and click bindings. Where it can identify the addon behind a conflicting binding, it names it directly, and it clears two known kinds of conflict for you automatically. This is the tool for the recurring question of why a key works on one character and does nothing on another. Different characters can carry different addon-set or click bindings on the same physical key, and this check surfaces that instead of leaving you to hunt through every addon's keybind panel by hand. If a fix does not stick, run /gems keys again and use Undo, which backs out the last change if something else grabbed the key in the meantime."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Tempo Advisor",
    "href": "/guide/settings#tempo-advisor",
    "body": "Settings > Faster/Slower This is a different system from the SQW Optimiser above, and it is worth being precise about the difference because the two get confused constantly in the Discord. The SQW Optimiser tunes a WoW CVar based on your latency. The Tempo Advisor analyses your actual sequence and your actual play to recommend a click rate, in milliseconds between keypresses, that fits how that specific sequence is built and how you actually play it. It starts from a theoretical estimate the moment you save a sequence: it classifies every spell into one of seven timing categories (off-GCD, on-GCD instant, on-GCD with a cast time, a long cast, a channel, a pause step, or unknown if the spell data is not cached yet), builds a transition graph between your steps, and sums GCD durations and cast times across that graph to estimate the time between presses your sequence actually needs. That estimate gets more accurate the more you play. After 30 logged samples from the addon's execution tracer, the Advisor blends real combat data into the recommendation at a 70% theoretical, 30% empirical mix, and discards stale samples automatically so an old patch or a respec does not keep skewing the number. Every sequence stores its own recommendation, sample count, and blend ratio, and it persists across sessions rather than resetting every login. Estimated Confidence level before 30 play samples are collected. The recommendation is theory-only at this point. Calibrated Confidence level once 30 or more samples are in. The recommendation now reflects how you actually play that sequence. Turn it on and a movable Faster/Slower overlay shows your current click rate against the recommendation, with a delta bar so you can see at a glance how far off you are. There is an optional rolling CPS sparkline if you want to watch your actual clicks per second over the last few presses, and an opt-in audio alert that plays a sound if your rate drifts too far from the recommendation, useful if you are not watching the overlay during a pull. /gems fs Show current Tempo Advisor status /gems fs on Enable the Faster/Slower overlay /gems fs off Disable the overlay /gems fs reset Clear stored data for a sequence and start fresh Run /gems fs reset after a major rework of a sequence, not just a small tweak. The Advisor is learning the timing of that specific step structure, and feeding it 30 samples from the old version before you swap to the new one will skew the recommendation until enough fresh data overwrites it."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Spell Cache Viewer",
    "href": "/guide/settings#spell-cache-viewer",
    "body": "/gems spellcache The cached spell data GRIP-EMS uses for icon resolution, autocomplete, and the Tempo Advisor's spell classification all lives in one place you can inspect directly. Run /gems spellcache , search by spell name or spell ID, and see exactly what the addon has stored: icon texture, cast time, cooldown, GCD category, and spell school. This is the tool to reach for when a spell icon is not resolving on your tracker or action bar, or when the Tempo Advisor is classifying a spell as unknown instead of one of its real timing categories. A stale or missing cache entry, usually from a spell getting renamed or reworked in a patch, is the most common cause of both. Search for the spell here first before assuming something is broken in your sequence itself. The cache populates on login and updates automatically when you change spec or talents. If a spell is genuinely missing rather than just stale, /gems revalidate forces a fresh re-tag of every sequence with current spell IDs, which usually resolves it."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "Outside programs: AHK, iCue, Synapse, and others",
    "href": "/guide/settings#outside-programs-ahk-icue-synapse-and-others",
    "body": "External programs that send repeated keypresses work fine with GRIP-EMS. A full keypress is a down event followed by an up event. Your program needs to send both. GRIP-EMS fires on the key-down signal specifically, so a program that sends only an up event or a custom hold signal will not trigger the sequence correctly. For interval settings: one full keypress every 50ms or slower is a sensible floor. Randomizing slightly, say between 50 and 75ms rather than a fixed number, produces cleaner behavior than a perfectly robotic fixed interval. The click rate guidance above applies here, too. Faster is not better past the point where your spells can actually land. The key-down requirement is the only thing that is not optional. Everything else is tuning."
  },
  {
    "page": "/guide/settings",
    "pageTitle": "Settings",
    "section": "How these settings connect",
    "href": "/guide/settings#how-these-settings-connect",
    "body": "The outside program sends keypresses at the rate you set. GRIP-EMS turns each keypress into one sequence step. The GCD is the actual ceiling on how fast spells can land, regardless of how fast you press. SQW controls the window in which your next press is queued into that GCD, and the SQW Optimiser ties that window to your real latency so it is always sized correctly for your connection. The Tempo Advisor is the layer above all of that: it watches your specific sequence and your actual play to tell you the click rate that fits both. Key Down Casting takes care of itself as of v2.3.14, so let the Optimiser handle SQW, and let the Tempo Advisor tell you where to set your click rate once it has enough data on you. Everything else on this page is fine-tuning from there. Back: Installation Next: How it works"
  },
  {
    "page": "/guide/how-it-works",
    "pageTitle": "How it works",
    "section": "The secure execution environment",
    "href": "/guide/how-it-works#the-secure-execution-environment",
    "body": "WoW runs addon code that interacts with combat in a restricted sandbox called the secure execution environment. Blizzard built this to prevent addons from automating decisions. Things like casting a spell when health is below 40% or using a cooldown when the boss is casting a specific ability are blocked because they would read arbitrary game state to make combat decisions. Inside a macro or sequence step, a meaningful portion of the Lua API is simply not available. This catches many new users who come from programming backgrounds and assume they can write logic into their sequences. The most common example is trying to check a resource value like combo points or holy power with UnitPower(\"player\") or timing logic with GetTime() . Both of those calls return nil inside a secure handler because they are part of the restricted API. The sequence does not error gracefully, it crashes. What you can use inside sequence steps is the standard macro conditional system that Blizzard has explicitly allowed: [combat] , [mod:shift] , [known:SpellName] , [noform:1] , [nochanneling] , and the rest of the documented macro conditional set. These are not API calls. They are tokens the macro engine parses directly and they are permitted because they do not read arbitrary game state. GRIP-EMS's Variables system exists partly to work around this limitation. Variables are resolved outside the secure environment before the macro compiles, which means you can use them to make conditional decisions that would be impossible inside a step directly. Buffs and debuffs are readable through that Variables system, and it is worth knowing exactly what is safe to check and what is not. HasBuff , HasDebuff , SpellReady , and SpellOnCooldown all hand back clean booleans you can build a variable around. What stays out of reach is anything numeric tied to the secret value system: Holy Power amount, combo point count, how many stacks of a buff you are holding, time remaining on anything. Those come back tagged in a way that throws the moment you compare or do arithmetic on them, and there is no trick around it, the CurveUtil approach some WeakAuras use gets tested against the same tag and fails the same way. If the number you actually want has an aura that only exists at that count, checking for the aura's presence instead of the number underneath it is usually the workaround. There is a second catch worth knowing before you build around any of this. A variable's value gets baked into your macro text once, at compile time, not read live on every press. Put UNIT_AURA in a variable's Events field and it re-evaluates when your auras change and queues a recompile, but that recompile writes to a secure button, which makes it combat locked, so it sits in the out-of-combat queue until you actually drop combat. A buff check built this way settles correctly at the start of a pull and then stays frozen for the rest of it. It does not chase a proc that comes and goes mid-fight. That makes Variables genuinely useful for anything that holds steady across a pull, a talent build, your spec, a raid buff, and not useful for gating a step on a short proc window."
  },
  {
    "page": "/guide/how-it-works",
    "pageTitle": "How it works",
    "section": "How the step engine actually advances",
    "href": "/guide/how-it-works#how-the-step-engine-actually-advances",
    "body": "GRIP-EMS is a Sequential step engine by default, which means it fires step 1, then step 2, then step 3, advancing one step per keypress and looping back to step 1 after the last step. The advance is unconditional. The engine sets up the step, hands the macro line to WoW, and moves the counter on. Whether the spell went out is not something it checks. What happens on the press is WoW's business. If a /cast names a spell that is on cooldown, the macro engine stops there and the cast lines below it in that same press never run, so the press produces nothing further. A /castsequence sitting on an entry that is on cooldown does the same thing. This is WoW reading your macro text rather than the sequencer making a decision, and it works identically under any addon that drives a macro. Conditional lines are the exception. A conditional that does not apply is skipped and the line after it still gets its turn. Both halves matter when you place a defensive. A press that cast nothing is not retried, and the step is spent until the loop comes back around. On a 30 step loop clicked every 150ms that is about 4.5 seconds, long enough for Ironfur to drop while the sequence walks the rest of the loop. Shorten the loop, move the step earlier, or give it a per-step interval."
  },
  {
    "page": "/guide/how-it-works",
    "pageTitle": "How it works",
    "section": "Proc-gated abilities",
    "href": "/guide/how-it-works#proc-gated-abilities",
    "body": "A step whose line names a spell you cannot cast right now produces nothing on that press, and the step advances anyway. There is no macro conditional that tests a proc. The documented set covers combat, modifiers, form, channeling, whether a spell is in your book and so on, but nothing that reads a buff, so you cannot write a step that fires only while a proc is up. Where WoW itself swaps the button to the proc version, name the base spell and the swap happens for you. Warrior's Slam becoming Heroic Strike under Bloodsurge is the old textbook case: /cast Slam gets you Heroic Strike while the proc is up, because WoW substitutes the override on the action and you never spend a press on a spell you do not have. The trap is writing /cast Heroic Strike instead, which casts nothing on every press where the proc is down. Adding a second spell after a semicolon does not rescue it either. A clause with no conditional in front of it is always true, so /cast Heroic Strike; Slam picks Heroic Strike every time and Slam never fires. If the proc version is a genuine override of the base spell, write the base spell. If it is a separate spell with its own availability, accept that some presses on that step do nothing, and keep anything else you need on that same step above the proc line, because a /cast that fails on cooldown stops the lines under it. [known:SpellName] is not a safe substitute for the override rule above when the spell in question is only ever granted by a talent or a buff rather than owned outright. It resolves through two different WoW APIs depending on how the game currently considers the spell available, and those two APIs can disagree about the exact same spell at the exact same moment. A step gated on [known:SpellName] for something like a temporarily-granted override can read as known by one check and not known by the other, which shows up as the conditional passing in the editor while the step does nothing in practice, or the reverse. Prefer the override rule, base spell name and let WoW substitute, over gating with [known:] whenever the spell is override-shaped rather than a talent you either have all game or not at all. One guard worth calling out on its own is [nochanneling] , which belongs on finisher steps like Rip or Final Verdict. That conditional is what stops the finisher from clipping a channel. Do not add [combat] on top of it, that causes silent failures."
  },
  {
    "page": "/guide/how-it-works",
    "pageTitle": "How it works",
    "section": "Step functions",
    "href": "/guide/how-it-works#step-functions",
    "body": "GRIP-EMS supports four step functions that control how the engine decides which step fires next. Sequential is the default and the one you will use for most rotations. Sequential Default Fires step 1, then 2, then 3, loops to 1. One advance per keypress. This is the correct choice for rotations where order matters, including tank defensive cycling, opener sequences, and anything where a spell at step 5 is supposed to come after the spells at steps 1 through 4. Priority Weights the loop toward the front. The steps are expanded into a longer cycle in which step 1 appears most often, step 2 slightly less often, and the last step once. Advancement is still one entry per keypress. Good for rotations where the early steps should get most of the presses. Reverse Priority The same weighting inverted, so the last step gets most of the presses and step 1 the fewest. In practice this means the tail of your loop fires far more often than the front of it. Avoid it unless that is genuinely what you want. Random Fires a random step each press. Useful for very specific situations like randomizing a proc-based spell into different positions to avoid predictable timing. Not useful for structured rotations. Priority is not a retry mechanism and it does not check whether a step is actually castable before choosing it, worth being precise about since the name invites the wrong mental model. It is a fixed pre-expansion computed once at compile time: for N steps you get N times (N plus 1) divided by 2 total slots in the cycle, arranged as step 1, then steps 1 and 2, then steps 1 through 3, and so on out to all N. A 4-step sequence compiles to 10 slots, step 1 taking 4 of them, step 2 taking 3, step 3 taking 2, step 4 taking 1. The engine then runs the exact same unconditional advance-on-every-press logic as Sequential over that expanded array. If step 1 is on cooldown, that press still does nothing, and the next press still takes whatever slot comes next in the array regardless of whether it is castable. Real fallthrough, where WoW tries one spell and only moves to a second if the first cannot fire, only exists inside a single step written as stacked /cast lines. Priority is a weighting tool for press frequency, not that. A Loop nested inside a Priority sequence does not count as one slot in the outer triangle. The compiler flattens the whole tree first, so a Loop unrolls into plain steps that land in the step list right alongside everything else, and Priority runs afterward over that flat list, it never sees the Loop as a unit. Two actions followed by a Loop of three actions repeating twice flattens to 8 steps, so Priority expands to 8 times 9 divided by 2, 36 slots, with the loop's own three actions graded against each other the same way any other steps would be, not as one weighted band. Set the Loop's own step function to Priority as well and it triangles its children first, feeding those already-weighted steps into the outer triangle on top, which compounds fast and is rarely what anyone actually wants."
  },
  {
    "page": "/guide/how-it-works",
    "pageTitle": "How it works",
    "section": "Understanding modifiers",
    "href": "/guide/how-it-works#understanding-modifiers",
    "body": "Modifiers are the single most common source of confusion for new users, and the confusion is almost always the same one: assuming SHIFT, CTRL, and ALT need their own separate keybinds somewhere. They do not. GRIP-EMS binds exactly one key to a sequence, in the Keybinds tab, and that single key is what you press or hold repeatedly. Modifiers ride on top of that same key rather than needing a bind of their own. Concretely: if your sequence is bound to the 1 key, you never bind SHIFT+1 or CTRL+1 anywhere. You hold SHIFT while pressing 1, and any step tagged with [mod:shift] fires instead of your normal rotation for that press. Release SHIFT and the next press goes back to firing the sequence normally. The same applies to CTRL and ALT. If you are looking for a place to bind SHIFT+1 or CTRL+1 specifically, stop looking. There is no such setting because that is not how modifiers work in GRIP-EMS. One keybind per sequence, modifiers layer on top of it. The guard pattern that makes this work correctly is [nomod:shift, nomod:ctrl] on your normal rotation steps. Without it, holding SHIFT for an emergency heal would also attempt to fire whatever spell is on that step, since the step has no way to know you only wanted the modifier action. Every regular rotation step should carry this guard if the sequence uses modifiers anywhere. The worked example on the Building sequences page shows this pattern applied consistently across a real 30-step sequence. If your keybind fires normally but a modifier does not, work through these in order before assuming something is broken in the sequence itself: 1. Check the step itself Open the sequence in the editor and confirm the step you expect actually has the [mod:shift] or [mod:ctrl] conditional written on it. A missing tag on the step is indistinguishable from a firing bug until you look. 2. Check CVar Health Run /gems settings, go to Cvar Health, and confirm it is green. The same key-down requirement that governs your base keybind governs modifier presses too. 3. Check your other WoW keybinds As of v2.4.13, GRIP-EMS’s keybind panel checks your sequence’s own bound key against your other WoW bindings, action bars, and other addons automatically, and anything holding a conflicting bind on that key is listed right there in the panel and in /gems keys. What the automatic sweep does not do is walk the SHIFT, CTRL, and ALT versions of that key on its own, so a clean result there only clears the bare key, not a modifier combination. For that, use the \"Check a key:\" box in the same panel and type the full combination, SHIFT-F3 rather than F3, and it names whatever owns that exact combination; modifier order does not matter, it sorts them for you, and it prints \"Checked as:\" underneath so you can confirm it read what you meant before you trust the answer. Before v2.4.13 none of this existed and conflicts had to be found by hand in WoW’s own keybind menu. A conflicting bind claimed by something else silently eats the modifier press before GRIP-EMS ever sees it, and it is a confirmed, recurring cause of dead modifiers, the automatic sweep just will not catch it if the conflict is sitting on a modifier combination rather than the bare key. 4. Reset all your WoW keybinds If steps 1 through 3 all check out clean and modifiers still are not firing, a full keybind reset in WoW itself (not just GRIP-EMS) has resolved this for other users even when no specific conflicting bind was ever found. It is a blunt fix and you will need to rebind everything afterward, but it works when nothing else does. If all four check out and modifiers still are not firing, post in the Discord with your CVar Health status, a screenshot of the step's conditional, and whether you have any other addon that binds modifier keys or touches CVars, so it can be looked at directly rather than retreading the same troubleshooting steps. One more limit worth knowing since it looks like a modifier bug when it is not: the 255 character cap sits on the combined text, not just the step's own line. Key Press, the step's action line, and Key Release get joined with newlines and the whole thing is measured together, per step. Bust that combined total and the step does not error and does not get dropped either, it falls back to running its own action line alone, with Key Press and Key Release left off for that one step only. Every other step in the sequence keeps them. GRIP-EMS tells you when this happens, a chat line on compile naming how many of your steps actually fit the modifiers, and the editor carries a matching fits-in label, so check for that before assuming a step with dead modifiers is broken. The one case that is a hard save-time error is the step's own action line alone going over 255 with no Key Press involved at all, that fails validation outright and will not save until you shorten it. Back: Settings Next: Features and behavior"
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "The Pause step",
    "href": "/guide/features-and-behavior#the-pause-step",
    "body": "GRIP-EMS includes a dedicated Pause step that holds the sequence without attempting a cast. It has three variants and they behave differently depending on what you need. Clicks Holds for a set number of keypresses before advancing. Good for spacing out high-cost abilities that need a fixed number of GCDs between them. Milliseconds Holds for a set duration regardless of keypresses. Useful when you need a hard time gate between steps rather than an action count. GCD Holds for one or more global cooldown cycles. The safest option for finisher spacing since it adapts to your actual GCD rather than a hardcoded time value. The Pause step is most commonly needed for specs with strict GCD relationships between abilities, for example preventing Steady Shot from firing too close to a proc window in Marksmanship Hunter. If you find a spell clipping something it should not, a one-GCD pause before that step is usually the fix to try first. As of v2.4.0, GRIP-EMS also has a dedicated Hold While Channeling setting, covered in full on the Settings page, which stops a mid-channel press from burning a step at all. It solves a related but different problem than the Pause step: Pause deliberately holds position for a set count or duration you choose, while Hold While Channeling reacts to an active channel and holds only for as long as that channel runs. Through v2.4.13 the setting only showed up for characters with an actual empowered spell in their spellbook, in practice meaning Evoker, even though the underlying hold has always worked on any channel. That was a UI gating bug, fixed in v2.4.14, so it is now the first thing to reach for on any channeled spell, not just an Evoker-specific tool."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Reset conditions",
    "href": "/guide/features-and-behavior#reset-conditions",
    "body": "Reset conditions send the sequence back to step 1. GRIP-EMS supports five of them and they can be combined. Reset on combat Resets to step 1 when you leave combat. Most tank and DPS sequences should have this enabled so your opener fires at the start of each pull. Reset on target Resets when your target changes. Useful for sequences with a target-specific opener that you want to replay on each new target. Reset on spec Resets when you change spec. Usually unnecessary if your sequences are spec-specific. Reset on gear Resets on gear swap. Relevant if you use gear sets that change your stat priorities mid-session. Timer reset Resets after a set number of seconds without a keypress. Useful as a fallback to catch sequences that got stuck mid-rotation during an interruption. Reset on target currently only takes effect out of combat. Switching targets mid-pull in Mythic+ will not reset the sequence to step 1 the way it will between pulls; the reset applies the next time you are out of combat and pick up a new target. If you were relying on a mid-combat target-change reset to replay a target-specific opener, it is not firing the way the setting name implies. Confirmed directly from the addon author; treat this as current behavior rather than an edge case, and design around Reset on combat for anything you need to trigger reliably inside a pull."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Skyriding and mount behavior",
    "href": "/guide/features-and-behavior#skyriding-and-mount-behavior",
    "body": "Pressing your sequence keybind while skyriding behaves differently depending on whether you have a valid target. Valid target below you One press dismounts and fires step 1 in the same action. No separate dismount required. No valid target The press falls through to your skyriding action bar as if GRIP-EMS is not active. Your normal skyriding controls are unaffected. This requires Auto Dismount in Flight to be enabled in your WoW settings. Druids also need Auto Unshift enabled to exit skyriding Travel Form mid-air with the same single press. Getting dazed off a skyriding mount mid-pull used to leave your sequence keys dead until you dropped combat. That is fixed. The swap to your ground action bar now happens the moment you land, combat-safe, so your keys are ready the instant you hit the ground. There was a separate bug on first takeoff of a session where ground binds stayed live mid-air or vehicle keys never woke up at all. That is also fixed. The out-of-combat watchdog now heals the swap at takeoff and restores it on landing, so the first flight of every session behaves the same as every other."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Context switching and multi-version sequences",
    "href": "/guide/features-and-behavior#context-switching-and-multi-version-sequences",
    "body": "A single sequence in GRIP-EMS can hold more than one version, and the addon picks which one is live based on what content you are in. GRIP-EMS recognizes dozens of distinct context types across raid difficulty, dungeon difficulty, Mythic+ key ranges, delve tiers, rated PvP, and more, and it checks on zone change, difficulty change, and group roster update. If you have built a separate version of a sequence for, say, Mythic+ versus raid, walking into a dungeon swaps you to that version automatically with no manual intervention. This used to have a real failure mode. Swapping versions by zoning into a dungeon or arena could drop a sequence's loop and branch grouping, and it would stay broken until you ran a manual /reload . That is fixed. The self-heal that rebuilds loop and branch structure now runs on the context switch itself, not just on a reload, so grouping survives the swap the moment it happens. If you want to override the automatic pick, you can pin a specific version as the live one regardless of what your current content or talents would otherwise select. The pin holds until you clear it, and the version list shows a badge next to whichever version is actually firing. Useful if you are deliberately running an off-spec version of a sequence, or testing a version before letting it take over automatically for its intended content. There is a second, separate way to make one key do different things depending on your setup, and it solves a different problem than context versions do. Per-loadout keybinds, found in Settings under General, lets a single keybind attach to whichever talent loadout was active when you set it, off by default and per character. Turn it on and any bind you make from then on belongs to that loadout specifically; binds you made before stay in place as the fallback for any loadout that does not have its own. The slash form is /gems bind <sequence> <key> --loadout <name or id> . The distinction that matters: context versions switch on where you are, zone, difficulty, key range, and so on. Per-loadout keybinds switch on which talent build you currently have equipped, regardless of zone. If your split is by content, a pet version for open world against a no-pet version for Mythic+, context versions is the tool, one sequence with two versions and the right context boxes ticked on each. If your split is by loadout instead, two full builds you swap between with the in-game loadout picker, per-loadout keybinds is the one that actually tracks it, since context versions has nothing loadout-aware in the picker today. If you use both loadouts in the same content, context cannot tell them apart and per-loadout keybinds is the only route that works."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Keybind recovery",
    "href": "/guide/features-and-behavior#keybind-recovery",
    "body": "GRIP-EMS includes automatic keybind monitoring. If your sequence keybinds go missing after a login, a loadout swap, or a deleted loadout eating its own binds, the addon detects it and tells you. Running /gems binds restore puts your last working set back immediately. The addon snapshots your binds on every clean load, so recovery is reliable even across sessions. If you see a warning about missing binds, run the restore command before assuming something is broken in your sequence. If your keys ever stop responding and you are not in a vehicle, a pet battle, or a cutscene, run /gems binds restore first. It takes two seconds and covers the most common cause of unexplained dead keys."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Per-step Disable and the sequence tracker",
    "href": "/guide/features-and-behavior#per-step-disable-and-the-sequence-tracker",
    "body": "Individual steps can be disabled inside the editor without deleting them. A disabled step is skipped entirely by the engine, which means you can comment out a step for testing purposes without losing the macro text. Re-enable it and the engine picks it up again on the next keypress. Disabled sequences are hidden from the tracker overlay and from your action bar. A sequence that is toggled off does not occupy a visible tracker slot, which keeps the display clean when you have multiple sequences loaded but only some of them active."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Viewing sequences from other characters",
    "href": "/guide/features-and-behavior#viewing-sequences-from-other-characters",
    "body": "GRIP-EMS sequences are stored per character. That is not a setting anyone picked, it is how WoW loads addon saved data: only the file belonging to the character you are currently logged in as gets loaded, so an alt's sequences are not hidden, they are not in memory at all while you are playing a different character. A brand new character showing an empty sequence list, with the search box finding nothing, is that working as intended rather than a bug. There is a way to see across characters without switching. The sequence list carries an Other characters tick box right under Current class only, off by default. Turn it on and you get a read-only view of every other character's sequences, names, icons, and step counts, once each character has logged in at least once on a build that has this feature; each character publishes its own list on login, so a fresh alt only appears in that list after you have logged into it once. You cannot fire another character's sequence from that view, but the copy to character option on any sequence stages it to actually land on that character the next time you log into it there."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Interleave / Weave",
    "href": "/guide/features-and-behavior#interleave-weave",
    "body": "Interleave lets you set an interval on any action so it fires every N steps automatically, without you having to manually place it throughout your sequence. Set an action's interval to 5, for example, and the compiler weaves that action into your rotation every fifth step, on top of whatever else is already there. This is the right tool for maintenance buffs, trinket procs, or cooldowns you want firing on a regular cadence without disrupting your main rotation flow. Rather than manually inserting the same spell at steps 5, 10, 15, and 20, you set one interleave interval and the compiler places it for you at every one of those points, correctly, even if you later add or remove steps elsewhere in the sequence. The real interval range is 1 to 50 steps. An interval of 1 weaves a copy in after every base step; 2, which is also the value a new interleave starts at when you first add one, is the most common source of people thinking 2 is the floor. It is only the default, not the minimum. Interleave does not require a Loop block. It works fine sitting at the top level of a sequence, and it works inside a Loop too, but the interval counts against a different population of steps depending on where it lives. At the top level it spaces against every compiled base step in the whole sequence. Inside a Loop, it spaces against that loop's own steps after its Repeat count has been unrolled, and only that loop's steps. The same interval number produces different real-world spacing depending on which of those two scopes it is sitting in, which is worth checking if an interleaved action seems to fire more or less often than you expected. There is a hard budget of 200 interleave copies across a sequence. Past that, later interleaves quietly get fewer copies inserted than you asked for rather than erroring, and it is easy to hit once a Repeat count on a loop duplicates the underlying list. If an interleaved action seems to be firing less often than its interval implies on a sequence with several other interleaves already running, that budget is worth checking before anything else. The editor marks any interleaved row with an [IL:N] indicator so you can see at a glance which steps are woven in versus part of your authored rotation. If your interval is larger than the block it lives in, the action never gets a chance to fire and compiles to nothing. GRIP-EMS now warns you when this happens, names the action, tells you the block's actual step count, and suggests an interval or Repeat count that would make it fit. If a trinket or buff you set up on interleave never seems to go off, check for this warning first before assuming the trinket itself is broken."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Plugin support",
    "href": "/guide/features-and-behavior#plugin-support",
    "body": "GRIP-EMS exposes a public plugin API so other addons can extend it without touching its source. Everything goes through one frozen entry point, GRIPEMS.API , and it is owner-scoped and isolated per plugin, so a bug in someone else's plugin breaks their plugin, not your sequences. Anything a plugin adds is owned by its plugin id and reverts cleanly the moment that plugin is disabled, no leftovers in your settings or your sequences. This is the kind of thing you will only ever notice if you run an addon that uses it. If a plugin adds a new export format, it shows up alongside the built-in one in the export window's format picker. If a plugin adds settings, they appear inside its own panel rather than scattered through GRIP-EMS's existing menus. None of this changes default behavior for anyone who is not running a plugin. As of v2.3.0, the API extends to action bars specifically. A plugin can put one of your sequences directly on an action button, reading per-step spell data, creating and picking up that sequence's macro, and registering its own /gems subcommand to go with it. Same rule applies: nothing changes unless you are running a plugin. If you build addons and want to extend GRIP-EMS yourself, the full API reference, including the security model and every method by access tier, lives at jesperlive.github.io/GRIP-EMS-PluginAPI . That documentation is the authoritative source for plugin development, this guide is written for sequence builders rather than addon authors, but the rest of this section covers enough to get a first plugin off the ground. If you want to build something that ships your own sequences, or hooks into GRIP-EMS more deeply than a settings panel can, the plugin API is the place to start, and it is a smaller lift than it looks. The docs site has a Getting started page and a working example under examples/MyFirstPlugin in the repo, and the wiki carries additional detail. A plugin is just its own addon that declares GRIP-EMS as a dependency in its .toc ( ## Dependencies: GRIP-EMS makes WoW load EMS first and refuse to load you without it) and calls into GRIPEMS.API from PLAYER_LOGIN rather than at file scope. Shipping a set of your own sequences is close to the smallest thing you can build with it. Check RequireVersion , then call API:RegisterSequences with a plugin name, a version string, an ordered list of sequence names, and a table keyed by those names. GRIP-EMS namespaces whatever you register under your plugin's own name, and a user's own sequence with the same name always wins on a clash, so a plugin cannot silently overwrite someone's personal work. Build each sequence inside GRIP-EMS itself and export it rather than hand typing the table, the exported shape is what RegisterSequences expects. RegisterSequences covers shipping fixed sequences, but a plugin that builds sequences on the fly, generating a rotation from a log or a stat weight rather than shipping a hand-authored one, needs to know the action tree: the five node shapes GRIP-EMS actually compiles a sequence's steps from underneath the editor's UI. action One compiled step: {type=\"action\", macro=\"/cast [combat] Kill Command\", interval=N} . interval is optional, 1 to 50, and weaves a copy of that action in every N steps, the same mechanism the editor's Interleave setting drives. loop Runs its children N times, 1 to 50, with its own step function: {type=\"loop\", children={...}, [\"repeat\"]=N, stepFunction=\"Sequential\"} . Lets a Sequential opener and a Priority body live in one sequence. if Branches on a macro conditional: {type=\"if\", variable=\"[combat]\", children={{true branch}, {false branch}}} . variable compiles down to [cond] on the resulting macro line, it is not a live runtime check. pause Emits N empty steps, a deliberate dead press for holding a GCD open: {type=\"pause\", clicks=N} . Anything below 1 clamps up to 1, there is no zero-click pause. embed Inlines another of the user's own EMS sequences by name at that point in the tree: {type=\"embed\", sequence=\"Name\"} . It reads that target sequence's already-compiled steps directly. A few of these are worth building defensively around rather than finding out about them from a bug report. An if node with an empty or unparseable conditional compiles to nothing, silently, no error shown to the user. Semicolon fallback syntax inside the conditional box is rejected outright, so validate whatever gets typed in there before it saves. A condition also only gets injected into commands the macro engine actually recognizes as conditional-capable; anything outside that set passes the line through unchanged and the condition is quietly dropped. An embed pointing at a sequence name that does not exist compiles to nothing just as silently, and since it reads the target's already-compiled steps rather than compiling them itself, embedding a sequence that has never actually been activated in EMS can also come back empty. Nesting is capped at 10 levels deep, on top of the 1 to 50 caps already mentioned on loop and pause . One field on a version is easy to get backwards: repeatCount . A value of nil , 0 , or 1 all mean no wrap at all, the sequence just cycles the normal way. Only 2 or higher does anything, and what it does is wrap the entire compiled action list in a synthetic Sequential loop and inline that many copies at compile time, capped at 50. The part that actually catches people: the version's own step function applies at runtime, over the already-duplicated list, not over your original one. repeatCount 3 combined with Priority runs the Priority expansion over the tripled list, so a 10-action sequence can balloon to 465 compiled slots rather than the number you would expect from Priority alone. Leave repeatCount at 0 unless the step function is Sequential. Two read calls are worth knowing if your plugin needs to inspect a sequence rather than just register one. API:GetAuthoredSteps(name) hands back the steps in the order you actually wrote them, loops unrolled and branches flattened, with no interleave copies. API:GetSequenceSteps(name) hands back the real execution order after the step function has expanded it, Priority triangle and all. Comparing the two lengths is a fast way to see whether a step function expansion or an interleave setting produced the count you expected. GetAuthoredSteps is newer than the current API version number suggests, so feature-detect it with if API.GetAuthoredSteps then rather than gating on RequireVersion alone. If you are building with an AI assistant, paste the AI context pack from the docs site into the conversation before asking for any code. Without it, a model will confidently invent API methods that do not exist, and you will spend real time debugging code that was never going to load. The docs site has a short guide on this exact failure mode with prompts you can copy. The rule worth keeping: if a method the AI wrote is not listed on that context pack page, it does not exist. The API keeps its own changelog, separate from the main GRIP-EMS changelog, tracking only what moved on the plugin surface itself. Worth a bookmark if you are maintaining a plugin, since a release can add a capability without bumping the API version number when the addition is purely additive, which means a presence check is sometimes the only reliable way to detect something new."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Importing sequences and macro name collisions",
    "href": "/guide/features-and-behavior#importing-sequences-and-macro-name-collisions",
    "body": "Every sequence import can carry a named WoW macro alongside it, the way the MOONSPAM pattern on the Building sequences page depends on. As of v2.4.3, GRIP-EMS checks whether it created a macro before it touches it during an import. If an incoming macro shares a name with one already in your /macro list that GRIP-EMS did not create itself, your macro is left alone and you get a chat line naming which one was skipped, rather than having its body silently replaced. Before v2.4.3, a name clash on the macro side was not handled the way sequence name clashes already were, and an import could overwrite a same-named macro you wrote yourself with no prompt and no undo. If you hit a skipped macro on import, rename either the incoming macro or your existing one and import again; the sequence itself still imports normally, only the colliding macro is held back. One rough edge as of v2.4.3: if you import while in combat, the import summary is written before the skip logic finishes running, so the reported count can read one macro high until combat ends and the actual chat line naming the skipped macro appears."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "The visual display layer versus what actually executes",
    "href": "/guide/features-and-behavior#the-visual-display-layer-versus-what-actually-executes",
    "body": "This is worth knowing because it causes real confusion in the Discord regularly. GRIP-EMS has two separate things: the visual preview of your sequence in the editor, and the compiled macro output that actually runs when you press your keybind. They are not the same thing. The visual layer renders steps it can match against known spells in its database. Steps it cannot match, including certain raw macro lines, some conditional constructs, and hero talent override spells under specific conditions, do not show in the preview. But they still exist in the compiled output and WoW's macro engine executes them correctly. A step that is invisible in the editor is not a broken step. This passthrough behavior is intentional and is how GRIP-EMS supports custom macro syntax that the addon's parser does not explicitly recognize. If you see fewer steps in the preview than you built and your sequence is otherwise working, this is almost certainly why. What gets written to disk when you save is a related but separate question from what gets displayed, and it has its own history worth knowing. Through v2.4.8, a step could get its stored spell swapped out from under you by whatever the game was calling that spell at the exact moment you hit save, rather than the spell you actually own. Blood DK running San'layn was the worst case of it, Heart Strike silently stored as Vampiric Strike while the buff happened to be up, and Ravage on Druid of the Claw had the same problem. Fixed in v2.4.9: EMS now stores the spell you actually own on both save and load, and any step that got corrupted this way before the fix corrects itself automatically on your next login. A second, differently-shaped version of the same category of bug affected talent-driven overrides specifically, where editing one version of a sequence could re-derive and overwrite the spell names stored in sibling versions you had not touched. v2.4.13 closed one of the three mechanisms behind that; v2.4.14 closed the remaining two. If you are on anything earlier than v2.4.14, treat any override or proc-swapped spell in a saved sequence as worth a manual check after an update, rather than assuming it survived untouched."
  },
  {
    "page": "/guide/features-and-behavior",
    "pageTitle": "Features and behavior",
    "section": "Sharing sequences and authorship",
    "href": "/guide/features-and-behavior#sharing-sequences-and-authorship",
    "body": "As of v2.4.8, GRIP-EMS actively protects authorship on anything you did not write yourself. Send a sequence to someone, answer a request for one, or run /gems export on it, and if you are not the original author, EMS names who is and waits for you to confirm before it goes out. That check follows the content itself rather than a copy count, so a copy of a copy still traces back to whoever actually wrote it. Your own work is completely unaffected by this, no prompt on anything you wrote, however many times you send it or however many copies exist. Two related fixes landed in the same release. Marking a sequence do-not-share used to fall off quietly the moment you duplicated it, saved it, renamed it, or retyped its Raw tab contents, and every sharing route would then treat the copy as shareable even though the original was not. The mark now survives all of that, and every sharing route refuses the copy exactly like it refuses the original. Separately, opening someone else's sequence and hitting Save, or duplicating one, used to silently re-stamp it with your name as the author. Both of those are gone; what EMS asks about now follows the actual content, not whichever account most recently touched the save button. Back: How it works Next: Building sequences"
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "The sequence: Elune's Chosen M+ V7.1",
    "href": "/guide/building-sequences#the-sequence-elune-s-chosen-m-v7-1",
    "body": "Author Slowdog Spec Guardian Druid Hero talent Elune's Chosen Content Mythic+ Step function Sequential Reset On combat Steps 30 Validated +13 Pit, +13 Darkflame Cleft, +13 Seat of the Triumvirate, +14 Ara-Kara, +14 Skyreach Talent string CgGA8cL7tpvige+kkmGM9zUPWDAAAAAAAAAAAgZmZmFzMjZWmZxMmZZZgZzMGNRmZWmZmZmlZmBAAAAAgZmNDYZbmBjZZAM1MLzyMzMAA2wMAWMGGYWssBYmZmNA This sequence runs 30 steps in a Sequential loop with resetOnCombat enabled, meaning it fires the opener every pull and loops through the rotation continuously from there. At 150ms intervals on Razer hardware this produces Thrash at roughly 47% of total damage done, Ironfur uptime in the 91 to 97% range, and zero deaths across the validated keys. Two modifiers run throughout the entire sequence. Shift fires Frenzied Regeneration on demand without breaking the loop. Ctrl fires Ironfur manually when you need it outside the automatic cycling. Every step except the two MOONSPAM steps and steps 15 and 30 carries both modifier lines so they are always available regardless of where you are in the rotation."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "Step by step breakdown",
    "href": "/guide/building-sequences#step-by-step-breakdown",
    "body": "The sequence has a clear internal structure once you see it: an opener block, then a repeating loop built around Thrash and Mangle with Ironfur cycling at fixed intervals, Lunar Beam weaved at positions 5, 12, 19, and 26, and MOONSPAM at positions 8 and 22 handling Moonfire delivery and Barkskin as a reset gate. 1 Opener: target acquisition, auto-attack, Bear Form /targetenemy [noharm][dead] /startattack /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur The opener does three things in one step. /targetenemy [noharm][dead] acquires a target if you do not have one or if your current target is dead, which is essential at the start of a pull when you are tabbing through packs. /startattack gets auto attacks rolling immediately. The Bear Form line shifts you in if you are not already in form, using [noform:1] as the guard so it does not waste a GCD when you are already a bear. resetOnCombat means this step fires every time you enter a new pull. Steps 2 through 30 are the loop. Step 1 is only ever the opener and not part of the repeating rotation. 2 Thrash /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur /cast [nomod:shift, nomod:ctrl, combat] Thrash Thrash fires first because it is the highest priority ability for Guardian damage and healing. It needs to land as early as possible in the pull and needs to refresh frequently throughout the loop. The [combat] guard on every combat spell in the sequence prevents the sequence from attempting Thrash out of combat during a pre-pull buffer and wasting the step. The [nomod:shift, nomod:ctrl] pair on the combat spells is what makes the modifier system work. Without those guards, pressing Shift for Frenzied Regeneration would also attempt to fire Thrash on the same step. 3 Incarnation + Mangle /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur /cast [combat] Incarnation: Guardian of Ursoc /cast [nomod:shift, nomod:ctrl, combat] Mangle Step 3 is the cooldown step. Incarnation fires immediately on the third press of the key, early enough to catch the initial Thrash tick buff and late enough that Bear Form is guaranteed from step 1. Notice that Incarnation uses [combat] only and not [nomod:shift, nomod:ctrl, combat]. This is intentional. Incarnation fires regardless of what modifier you are holding because you never want to accidentally skip it by holding Shift for a Frenzied Regen at the wrong moment. Mangle follows as the filler if Incarnation is on cooldown or already active. 4 Thrash /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur /cast [nomod:shift, nomod:ctrl, combat] Thrash Thrash again. The pattern throughout this sequence is Thrash, Mangle, Thrash, Mangle with Ironfur and MOONSPAM interspersed at calculated intervals. Thrash appears 11 times across the 30-step loop, which is what produces the 47% damage done figure in logs. 5 Lunar Beam + Mangle /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur /cast [known:Lunar Beam, combat] Lunar Beam /cast [nomod:shift, nomod:ctrl, combat] Mangle The first Lunar Beam weave. [known:Lunar Beam] is the conditional that makes this build-agnostic. If the talent is not taken, the line silently skips and Mangle fires instead. Lunar Beam appears at steps 5, 12, 19, and 26, which spaces it at roughly every 7 steps through the loop to align with its cooldown. 7 Ironfur (in-sequence) /cast [noform:1, nochanneling] Bear Form; [mod:shift] Frenzied Regeneration; [mod:ctrl] Ironfur /cast [nomod:shift, nomod:ctrl, combat] Ironfur The first in-sequence Ironfur cast. Ironfur appears at steps 7, 14, 21, and 28, every 7 steps, intentionally aligned with its duration so the buff refreshes before it falls off. This is what produces the 91 to 97% uptime in logs. The math only works if the sequence actually reaches these steps at the right pace, which is why loop length and a steady click rate matter so much for tank rotations specifically. 8 MOONSPAM: Moonfire delivery and Barkskin /castsequence [nomod, nochanneling] Moonfire, Barkskin /cast Ironfur This is the most important step in the sequence and the one that confuses people most when they first read it. MOONSPAM is a named WoW macro referenced by GRIP-EMS as a macro step, not a regular action step. The /castsequence fires Moonfire on the first pass, then Barkskin the next time this step is reached at step 22, then resets to Moonfire again. Barkskin's roughly 60 second cooldown means it fires naturally at roughly the right frequency when paired this way without needing any cooldown tracking. The reason this lives in a WoW macro rather than directly in a GRIP-EMS step is castsequence state. WoW's castsequence tracks its internal position, meaning which spell in the sequence comes next, inside the macro itself. That state persists correctly only when the castsequence lives in a named WoW macro. If you copy the /castsequence line directly into a GRIP-EMS action step, the state resets on every keypress and Moonfire fires every time instead of alternating with Barkskin. The WoW macro reference is what preserves the Moonfire, Barkskin alternation across the full 30-step loop. The [nomod, nochanneling] conditionals on the castsequence line are both necessary. [nochanneling] prevents the step from interrupting an active channel. [nomod] prevents the castsequence from firing when you are holding Shift or Ctrl for Frenzied Regeneration or Ironfur, so modifier presses on this step do not accidentally advance the castsequence position. The bare /cast Ironfur on the second line fires unconditionally as an extra Ironfur attempt since Ironfur is off-GCD and costs nothing to attempt. To use this sequence you need to create the MOONSPAM macro first in WoW's regular macro editor containing exactly these two lines: /castsequence [nomod, nochanneling] Moonfire, Barkskin /cast Ironfur Then in GRIP-EMS, click Add at the bottom of the steps list, select Macro from the dropdown, and pick MOONSPAM from the dialog that appears. It will show as [M] MOONSPAM in your sequence list. The name must match exactly, including capitalisation. Steps 9 through 30: the loop continues Steps 9 through 30 repeat the same pattern established in steps 2 through 8: Thrash and Mangle alternating, Ironfur at steps 14, 21, and 28, Lunar Beam weaved at steps 12, 19, and 26, MOONSPAM at step 22, and a bare Ironfur at steps 15 and 30 without the nomod guards so it fires regardless of modifier state. Step 30 ends the loop and resets to step 2 on the next press."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "The KeyPress field and what it is for",
    "href": "/guide/building-sequences#the-keypress-field-and-what-it-is-for",
    "body": "GRIP-EMS has a KeyPress field that fires on every single keypress regardless of where the sequence is in its step list. It is not a step in the rotation. It is a persistent header that runs before every step fires. Modifier handling belongs here and only here. If you put [mod:shift] lines inside individual steps instead of in KeyPress, every step has to carry that conditional which eats into the 255-character step limit and reduces CPM across all abilities. Putting modifiers in KeyPress means a single line handles the modifier for every press and your steps stay clean. There is a meaningful secondary benefit. When modifiers live in KeyPress, the Rotation Preview panel in the GRIP-EMS editor can display your actual spell names instead of showing only the raw conditional text. If your sequence preview shows /cast [mod:shift] instead of the spell name, the modifier is in the steps rather than in KeyPress. Do not put combat spells in KeyPress. Anything in KeyPress fires on every single keypress including before combat, during movement, and any other context where you press the key. Modifiers and channel-stop lines belong there. Rotation spells belong in steps."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "The reset=target castsequence pattern",
    "href": "/guide/building-sequences#the-reset-target-castsequence-pattern",
    "body": "Some abilities should fire once on each new target rather than on a timer or as a rotation step. Hunter's Mark is the canonical example. You want it to apply when you engage a target, reapply when you switch targets, and not spam continuously on every keypress. The correct pattern for this in GRIP-EMS is a castsequence with a target reset, placed in a step near the top of the loop: /castsequence [nochanneling,exists] reset=target Hunter's Mark, null This fires Hunter's Mark on the first press after you acquire a target, then advances the castsequence to null , which is a no-op. The sequence stays on null for every subsequent press against that target, so Hunter's Mark does not repeat. When you switch targets the castsequence resets to Hunter's Mark and fires again on the first press. The [exists] conditional prevents the line from attempting to fire when you have no target. The [nochanneling] prevents it from interrupting a channel. Both guards are necessary for the pattern to work cleanly in M+ where target switching is frequent. The same pattern works for any ability you want to fire once per target: Faerie Fire, Misdirection to a specific target, any debuff that should land on engage and reapply on target change. Swap Hunter's Mark for the ability name and the behavior is identical. Do not put Hunter's Mark in KeyPress. KeyPress fires on every keypress, so Hunter's Mark would attempt to cast continuously. The reset=target castsequence is the correct approach."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "Hierarchical actions: when to use the node editor instead",
    "href": "/guide/building-sequences#hierarchical-actions-when-to-use-the-node-editor-instead",
    "body": "Everything in this guide so far writes conditional logic directly into macro text, using bracket conditionals like [mod:shift] and [nomod:shift, nomod:ctrl, combat] right inside each step. That is a deliberate choice. It keeps you in full control of exactly what compiles and exactly how many of your 255 characters it costs, and it is the pattern the Elune's Chosen example above uses throughout. But it is not the only way GRIP-EMS lets you build conditional logic, and for some patterns it is not the right tool. Beyond the flat step list, GRIP-EMS has a hierarchical action tree with a visual node editor. Five node types are available: Action, which is a single macro step and the leaf of the tree; Loop, which repeats its children a set number of times or indefinitely; Repeat, which runs its children N times and then moves on; Conditional, which evaluates a Lua condition before running its children; and If, ElseIf, and Else, which give you proper branching with multiple condition paths instead of chaining bracket conditionals by hand. An If node compiles a True branch and a False branch into a single chunk of macro conditional text, the same kind of output you have been reading throughout this page, except the editor builds it for you from whatever you put in each branch. If your True branch is empty, the compiler does not emit a clumsy empty-clause-with-semicolon-fallthrough. It automatically applies De Morgan negation and emits the shorter negated form instead, the same way [noindoors] reads cleaner than an empty bracket followed by a semicolon. A path indicator badge next to the Cond box tells you which compile path the node took, Single line, Two-line split, Per-step, or Optimised fall-through, and the Compiled Output preview pane shows you the literal macrotext the engine will actually run. That preview is the real value here: it lets you verify exactly what is going to fire before you save, which is easy to get wrong reading raw bracket syntax by eye once a branch has more than one action in it. That last point is the actual reason to reach for the node editor instead of hand-written brackets. A single toggle, like the Shift modifier pattern used throughout the Guardian Druid sequence above, is faster to write and easier to scan as a flat inline conditional, which is why this guide teaches it that way. But once a branch needs more than one action, an opener that casts two or three different spells depending on whether you are indoors, mounted, or in a specific stance, hand-chaining semicolons gets error-prone fast and a single mistake silently breaks the branch. The visual tree keeps each branch's actions grouped and editable on their own, and the Compiled Output preview catches mistakes before they reach your action bar instead of after. If you are building something more involved than a single modifier toggle, multiple actions per branch, nested conditions, or logic you keep getting wrong reading it back as bracket text, switch to the node editor for that part of the sequence rather than fighting it in raw macro syntax. The two approaches compile to the same kind of macro conditional output underneath, so you are not choosing a worse system, just a different editing surface for more complex logic."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "Charged and empower spells: why they break automated sequencing",
    "href": "/guide/building-sequences#charged-and-empower-spells-why-they-break-automated-sequencing",
    "body": "Charged spells and empower spells, Evoker's Fire Breath and Upheaval being the clearest examples, behave differently from a normal cast and that difference is what causes automated sequences to hitch, freeze, or feel wrong even when every step looks correct on paper. A normal spell either casts or it does not. A charged or empower spell has stages: holding the button charges it up, and releasing early locks in whatever charge level you have reached so far. Mechanically each charge level is treated as a different spell, and moving between them stops the previous cast rather than smoothly continuing it. When that stop happens automatically instead of on your own release, it costs a hitch, roughly a full GCD of dead time, before anything can be cast again. This is not a bug in GRIP-EMS. It is the same behavior press and hold casting has always had with charged spells, and automating the press does not remove it. If you let press and hold run its full charge without ever cutting it off early, there is no hitch. The problem only shows up when a sequence cancels the charge partway through, which is exactly what happens if you try to time charge level via pause steps or spam intervals instead of holding to completion. As of v2.4.14, start with the setting built for exactly this problem before reaching for either workaround below. The other two approaches here predate that fix and exist specifically to route around the automated-cancel hitch by hand; the setting solves it directly instead. Works, and try this first: Hold While Channeling Covered in full on the Settings page. This setting, and specifically its Hold + release at max option, holds a press landing mid-channel instead of letting it cancel the charge early, then releases at the point you asked it to. Through v2.4.13 it only appeared for characters with an actual empowered spell in their spellbook, in practice Evoker, even though the underlying mechanism has always worked on any channel. That was a UI bug, fixed in v2.4.14, so it now applies cleanly to Fire Breath and Upheaval without the guard-step or separate-key workarounds below. Also works, predates the setting above: a dedicated first-priority guard step Add a step near the top of the loop, before your main rotation logic, that only fires while the spell is actively channeling: /cast [channeling:Upheaval] Upheaval /cast [channeling:Fire Breath] Fire Breath This keeps re-issuing the cast on every keypress for as long as the spell is already channeling, which is what lets it run to completion instead of getting cut off by whatever the next step in the loop would otherwise fire. It is not a perfect fix. Whether WoW correctly recognises the empower as channeling versus treating it as its own separate cast state is inconsistent, so expect to still feel it as slightly wonky rather than seamless. Also works, predates the setting above: fully separate sequences per charge spell, on separate keys Rather than folding a charged spell into your main rotation sequence, give it its own standalone sequence bound to its own key, for example Upheaval on Alt+0, Fire Breath on Shift+0, with your main rotation on 0. You manually decide when to reach for the charged spell and hold through its full charge, while the base rotation sequence keeps running independently on its own key. This is more deliberate play than a single fire-and-forget button, but it is the only approach that avoids the automated-cancel hitch entirely, since nothing is ever cutting the charge off early. Does not work well: timing partial charge levels via pause steps It is possible to count how many presses at a given click rate correspond to a specific charge level and build pause steps to try to land there deliberately, so the sequence releases the spell at, say, charge 2 instead of running it to completion. In practice this needs an impractical number of precisely-timed presses, does not survive a mid-combat reset since the sequence has no way to know which pause block it left off on if a fight interrupts it, and the next attempt ends up re-entering the pause sequence at the wrong position, causing a delay before the tap actually registers followed by having to work back through the pause blocks again. Treat this as a known dead end rather than something to debug further. None of this is Evoker specific in principle, it applies to any spell WoW treats as charged or empowered, but Evoker is currently the class where it comes up. If you are building a sequence around Aug Evoker's Ebon Might upkeep or a Devastation opener that leans on Fire Breath or Upheaval, start with Hold While Channeling and fall back to the guard-step or separate-key approach only if it does not behave the way you need for your spec, rather than assuming a normal cast-step pattern will behave the same way it does for every other ability in your kit."
  },
  {
    "page": "/guide/building-sequences",
    "pageTitle": "Building sequences",
    "section": "The decisions that matter",
    "href": "/guide/building-sequences#the-decisions-that-matter",
    "body": "Why Sequential and not Priority Priority does not try Thrash and fall through to something else if it can't fire, that is not what the step function does; it just gives Thrash more of the fixed slots in a pre-expanded cycle, so it lands in front of the keypress more often. It has no idea whether Thrash is actually castable when it hands you that slot. Weight the cycle toward Thrash and the presses that land on a Thrash slot while Thrash is on cooldown produce nothing, and Ironfur, Mangle, and Lunar Beam only ever get the presses left over from however that cycle happened to fall, which is not the same thing as firing when Thrash is on cooldown. Ironfur uptime built on that is accidental, not structured. Sequential gives you predictable step positions and predictable uptime instead. Why [combat] on every spell Without [combat] guards, spam-pressing the key before a pull would advance the sequence through multiple steps before combat starts, meaning you would enter the fight at step 4 or 5 instead of step 1. The opener only fires correctly if the sequence is at step 1 when the first combat spell lands. Why Thrash at 11 of 30 steps Thrash is both the primary damage source and the primary self-healing source for Guardian Druid running Soul of the Forest. More Thrash means more healing. The sequence is built around maximizing Thrash frequency while still fitting Ironfur cycling and the mandatory cooldowns. 11 steps out of 30 is the result of that tradeoff after log validation. Why Ironfur every 7 steps Ironfur's base duration is 7 seconds and the sequence runs at roughly one step per GCD at 150ms. Placing Ironfur at every 7th step means a new Ironfur cast lands approximately when the previous one expires. This is the mechanism behind the 91 to 97% uptime. If you remove Ironfur steps or change their spacing, uptime drops and it shows immediately in logs. ← Back: Features and behavior Next: Coming from the legacy program →"
  },
  {
    "page": "/guide/from-legacy-program",
    "pageTitle": "Coming from the legacy program",
    "section": "Getting your sequences into GRIP-EMS",
    "href": "/guide/from-legacy-program#getting-your-sequences-into-grip-ems",
    "body": "This is probably what you came here for first. GRIP-EMS imports sequences from the legacy program automatically and the process takes about two minutes. Import reliability has improved significantly in recent releases, so if you tried this previously and had issues, it is worth trying again. Option 1: In-game migration (recommended) If the legacy program is still installed alongside GRIP-EMS, open the editor with /gems and click Migrate in the sequence list. GRIP-EMS detects your legacy sequences and transfers everything automatically, including steps, variables, metadata, and multi-version data. A report in chat tells you what came across and what, if anything, needed attention. Option 2: Clipboard import Export a sequence from the legacy program to your clipboard, then run /gems import in GRIP-EMS and paste the string. GRIP-EMS auto-detects the format, shows you a preview with metadata and a checksum status, and lets you handle any naming conflicts before importing. After importing, run /gems repairall to scan every transferred sequence across 13 diagnostic categories. Most issues from format differences get flagged and fixed automatically. This takes about thirty seconds and saves you from discovering problems mid-pull. Sequences from the legacy program frequently overshoot WoW's 255-character step limit because it builds longer individual step strings than GRIP-EMS allows. The Repair module flags these on import and the fix is to split the oversized step into two shorter steps carrying the same spells. This is one of the most common issues when porting sequences from the legacy program, so if repair comes back with character limit violations do not be alarmed, it is normal and fixable in a few minutes. Sequences shared in English by another player import and translate to your client language automatically. Spell names stored as IDs under the hood re-render in your locale on import, so a sequence built on an English client works for German or French players without any manual editing."
  },
  {
    "page": "/guide/from-legacy-program",
    "pageTitle": "Coming from the legacy program",
    "section": "How advancement actually works",
    "href": "/guide/from-legacy-program#how-advancement-actually-works",
    "body": "Both engines advance one step per keypress and neither one waits for a cast to land. What can make a press do nothing is the macro line on the step. If a /cast names a spell that is still on cooldown, WoW stops running that macro there and the lines under it never fire, so the press comes up empty and the step advances anyway. A /castsequence parked on an entry that is on cooldown behaves the same. That is the WoW macro engine reading your text, so you get it under either addon. Conditional lines are different: a conditional that does not apply is skipped and the next line still runs. The practical consequence is that loop length is a real cost. In a flat sequential loop every step gets one visit per pass, so a 30 step loop clicked every 150ms is roughly 4.5 seconds between visits to any single step. A press that came up empty is not retried, and that step is spent until the loop comes back around. If a defensive needs to come around faster than that, shorten the loop, move the step earlier, or give it a per-step interval. Advancement being deterministic is what makes a sequence readable. One press is one step, so a step position means something and you can reason about the rotation by reading it top to bottom. It is also what makes log comparison useful. Two runs of the same sequence at the same click rate walk the same steps in the same order, so a difference in the numbers points at the sequence or the pull rather than at the engine."
  },
  {
    "page": "/guide/from-legacy-program",
    "pageTitle": "Coming from the legacy program",
    "section": "What is different between the two addons",
    "href": "/guide/from-legacy-program#what-is-different-between-the-two-addons",
    "body": "GRIP-EMS Legacy program Action bar button Single-version sequences have no bar button. Multi-version sequences create a macro you can place on your bar. Creates a draggable button you place on a bar Keybinds Assigned inside GRIP-EMS per spec, auto-switch on spec change Via the action bar button you place and bind Import format !EMS1! format, import legacy program strings with /gems import Base64 string with version prefix Step functions Sequential, Priority, Reverse Priority, Random Sequential, Priority, and others depending on version Opener logic True single-block loop, step 1 is only the opener Block 1 fires between every loop step when compiled, not just once Spell validation Built-in scanner with patch-aware auto-translation Limited or absent depending on version Click rate guidance Tempo Advisor learns from your actual play and recommends a click rate per sequence, with a live Faster/Slower overlay None built in Post-patch repair Repair module fixes stale spells in one click Manual identification and replacement Cross-language sharing Spell IDs stored internally, renders in recipient's language Spell names in source language, may not fire on other clients"
  },
  {
    "page": "/guide/from-legacy-program",
    "pageTitle": "Coming from the legacy program",
    "section": "Things that trip up switchers specifically",
    "href": "/guide/from-legacy-program#things-that-trip-up-switchers-specifically",
    "body": "Looking for the action bar button The legacy program creates a macro button you drag to your action bar and bind to a key. GRIP-EMS works differently. You assign a keybind inside the addon directly. For single-version sequences there is nothing to drag. For sequences with multiple versions, GRIP-EMS creates a macro that can be placed on your bar, but the keybind is still assigned inside the addon. Open the sequence editor, go to the Keybinds tab, and press the key you want. Keybind set but nothing fires The legacy program works fine with WoW's default key-up event behavior. GRIP-EMS requires key-down. As of GRIP-EMS v2.3.14 this is forced on automatically, so update through your addon manager first. If you are still on an older version, run /gems settings, go to the Cvar Health tab, and click Fix if anything is not green. This used to be the most common reason a switcher imported a sequence, pressed the keybind, and got nothing; on 2.3.14 and later it should not come up at all. Multi-block opener logic behaving unexpectedly In the legacy program, putting your opener in Block 1 and your main rotation in a Loop block seems like clean architecture, but Block 1 fires between every loop step when the sequence compiles, not just once at the start. Opener spells end up firing far more often than intended. GRIP-EMS does not have this problem because Sequential step function advances linearly. Step 1 is step 1 and not a recurring block. If you are porting a sequence that used this pattern, rebuild it as a flat Sequential loop in GRIP-EMS. Reverse Priority for finisher steps Reverse Priority is a common pattern for DPS rotations that want finisher spells to fire when available. In GRIP-EMS it weights the loop toward the tail, so the last step gets most of the presses and the front of the loop gets very few. That is rarely what a finisher rotation wants. If you are porting a sequence that used Reverse Priority for finishers, rebuild it as Sequential with the finisher placed where you want it in the loop, or use Priority and put the spells that should get the most presses at the front. Spell names after a patch Blizzard renames and reshuffles spells with some patches, and sequences that were working silently stop working because a spell name no longer matches. GRIP-EMS scans for this automatically and flags broken steps with a red indicator in the editor. Run /gems validate after any patch that touches your spec and /gems repairall to fix what it finds. Most stale spells are resolved automatically without any manual editing."
  },
  {
    "page": "/guide/from-legacy-program",
    "pageTitle": "Coming from the legacy program",
    "section": "You do not have to choose permanently",
    "href": "/guide/from-legacy-program#you-do-not-have-to-choose-permanently",
    "body": "Both addons can be installed at the same time and the sequence formats do not cross-contaminate. A reasonable approach is to move one spec across first, the one whose sequence you edit and tune the most, and keep your existing legacy program sequences for everything else until you have decided the tooling is worth the move for those specs too. Translating sequences between the formats is not automatic, but the underlying macro logic is the same since both addons use WoW's standard macro conditional syntax. A sequence from the legacy program can be rebuilt in GRIP-EMS step by step without starting from scratch. Step spacing and timing can still shift once the step count or the click rate changes, so plan on a validation pass after porting rather than assuming the numbers will be identical. Building sequences Next: Validating your work"
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "Start with the Repair module",
    "href": "/guide/validating#start-with-the-repair-module",
    "body": "Before you run any content, run the Repair module. It is the fastest way to catch structural problems that would otherwise waste a key or a raid attempt finding out. GRIP-EMS scans your sequence across 13 diagnostic categories: empty steps, oversized steps that exceed WoW's 255-character limit, stale or renamed spells, duplicate steps, missing variables, broken reset conditions, keybind conflicts, missing metadata, and more. Each issue gets flagged in the editor with a colored health score badge and most can be fixed in a single click. /gems repair <name> Scan and repair a single sequence by name /gems repairall Scan every sequence you own in one pass /gems validate Check all sequences specifically for stale or renamed spells /gems revalidate Force a full spell revalidation, run this after a patch A clean repair pass with a green health score badge means the sequence is structurally sound. It does not mean the step ordering and timing are optimal for your spec, and that is what logs are for. Run repair first, then run content, then check logs. Run /gems repairall after every game patch that touches your spec. Blizzard renames and reshuffles spells with some patches and sequences that were working silently stop working because a spell name no longer resolves. The Repair module catches these and fixes most of them automatically."
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "Why logs are the proof standard",
    "href": "/guide/validating#why-logs-are-the-proof-standard",
    "body": "When you are iterating on a sequence, you are making decisions about step order, step frequency, and timing. Your gut tells you the sequence felt good, but your gut does not know whether your primary maintenance buff was up for 94% of the fight or 71%. Logs do. The specific problem with tank and buffer validation is that the consequences of a bad sequence are sometimes invisible in the moment. A sequence with poor defensive uptime does not feel dramatically different on a plus 10 where you are significantly overgearing the content. It shows up on a plus 13 when the healer goes dry covering gaps. Logs let you find those gaps before the key tells you about them the hard way. Target dummies have none of the variables that real content introduces: movement, interrupts, crowd control, latency spikes, or the reaction time that interrupts your keypress rhythm. A sequence that looks perfect on a dummy degrades in live content in ways that are only visible in logs. That is where the gap between a well-structured sequence and a poorly-structured one becomes measurable, because a step you press while moving or stunned still advances and the spell it named simply does not go out."
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "The validation framework for any spec",
    "href": "/guide/validating#the-validation-framework-for-any-spec",
    "body": "The process is the same regardless of what class you play. The targets you are looking for differ, but the method does not. 1 Identify your two or three most important spells Before you run anything, know what you are measuring. For a tank this is your primary defensive buff and your main damage ability. For a DPS spec it is your highest priority spell and your major cooldown. Check Icy Veins or your spec Discord for the expected casts per minute of each. These are your targets. 2 Enable Advanced Combat Logging before the run In-game, type /combatlog or go to System > Network and enable Advanced Combat Logging. This must be on before you enter the instance. If you forget, the log will not contain the data you need for cast analysis. 3 Run at least two sessions at relevant difficulty Run the content at the difficulty level you are building the sequence for. A plus 10 gives you different data than a plus 13 because mob damage, movement requirements, and latency pressure are different. Two runs at the same difficulty lets you see whether your numbers are consistent or varying, which tells you whether the sequence is stable. 4 Upload to Warcraft Logs and pull your cast data Upload your combat log at warcraftlogs.com, navigate to your report, click on your character to filter to your casts, and go to the Casts tab. Export the CSV. This gives you spell name, cast count, and cast time for every ability you used. 5 Compare against your targets Filter the CSV by each of your key spells. Divide cast count by fight duration in minutes to get casts per minute. Compare against your Icy Veins or SimCraft targets. If you are within 15% of the expected number, the sequence is in the right range. If a spell is more than 20% below target, there is a structural problem. If a spell drops to zero casts entirely, something is blocking it and it needs immediate attention. 6 Adjust and rerun When you find a number that is out of range, trace it back to the sequence structure. A maintenance buff with low uptime usually means the loop is too long for the buff duration, so the step that refreshes it does not come around often enough. It can also mean the refresh sits under a /cast that is often on cooldown, because a failed cast stops the lines below it on that press. A major cooldown firing late usually means resetOnCombat is disabled or something is advancing the sequence pre-pull. Make one structural change at a time and rerun before making another, because changing multiple things at once makes it impossible to know which change fixed the problem."
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "Worked example: applying the framework to a real build",
    "href": "/guide/validating#worked-example-applying-the-framework-to-a-real-build",
    "body": "The numbers below come from five validated keys at plus 13 and plus 14 with the Elune's Chosen Guardian Druid sequence published on this site, and they are here because a concrete example of what the framework actually produces is more useful than a generic description of what it could produce. Your spec will have different spells, different uptime targets, and different CPM expectations and none of these numbers are benchmarks you are trying to match, and if you are playing anything other than Guardian Druid they are not relevant to you directly. What is relevant is the shape of the table: two or three key metrics, a specific target range for each, and a clear diagnostic if the number falls outside it. That structure is the same for every spec. Metric Target What it tells you Thrash % of total damage ~47% Primary damage and healing source. Below 40% means step spacing is off or Thrash is failing too often. Ironfur uptime 91 to 97% Anything below 85% on a single-target fight is a structural problem. Check the step spacing at positions 7, 14, 21, and 28. Moonfire CPM ~1.3 CPM Above 3 CPM means MOONSPAM is misconfigured or replaced with a direct /cast Moonfire. The castsequence gate exists specifically to hold this number. Incarnation timing 3rd keypress Should fire on the third press of every pull. Firing later means something is advancing the sequence before combat starts. Mangle presence Regular throughout If Mangle disappears from the log entirely, a misconfigured conditional is blocking it. To build the equivalent table for your spec, take your two or three highest value spells from the Icy Veins priority list, find their expected CPM or uptime percentage from SimCraft or your spec Discord, and use those as your targets. The diagnostic logic is the same: if a spell is significantly below target, trace it back to the step structure."
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "What bad numbers tell you",
    "href": "/guide/validating#what-bad-numbers-tell-you",
    "body": "A high-priority spell has significantly lower CPM than expected Step spacing is wrong, or the spell sits below a /cast that is frequently on cooldown, which stops the rest of that press before it reaches your spell. Count how many times that spell appears in your step loop and compare to total steps. If it appears at the right frequency in the sequence but not in the logs, look at the lines above it on that step. A maintenance buff shows below 80% uptime Steps for that buff are too far apart in the loop, or [combat] guards are incorrectly blocking it. Check that the buff step does not carry a [combat] conditional that prevents it from firing when you need it, and verify the spacing matches the buff duration. Your major cooldown is firing late or not on pull resetOnCombat is disabled so the sequence starts mid-loop, or the sequence is advancing pre-pull due to missing [combat] guards on early steps. Check both. Numbers look fine on a dummy but fall apart in keys Dummies have no movement, interrupts, or latency spikes. On a dummy every press produces a cast, so the loop looks perfect. In a key the presses you spend moving or stunned still advance the sequence and produce nothing, which is where a fragile sequence falls apart and the dummy gives you a false positive. A spell drops to zero casts entirely A conditional is misconfigured and blocking the spell completely. Check for typos in the spell name, a [known:] conditional for a talent you do not have, or a modifier guard that is preventing the spell from firing under any conditions. The editor shows an orange warning on a spell that is actually working Some abilities change names mid-combat based on talents or procs, like Raptor Strike becoming Raptor Swipe, Slam upgrading to Heroic Strike via Bloodsurge, or hero talent overrides replacing the base spell name. GRIP-EMS flags these because the stored name no longer matches what is in your spellbook, but the spell still fires correctly. Run /gems repair to confirm the sequence is structurally sound. If repair comes back clean and the spell is casting in logs, the warning is cosmetic and safe to ignore."
  },
  {
    "page": "/guide/validating",
    "pageTitle": "Validating your work",
    "section": "Validating a sequence you did not write",
    "href": "/guide/validating#validating-a-sequence-you-did-not-write",
    "body": "When you import someone else's sequence, run the same validation process before relying on it in serious content. Published sequences are validated against specific talent builds and specific content levels, and those conditions may not match yours exactly. A sequence validated on one hero talent path with one set of tier bonuses will produce different numbers on a different configuration because the spells and their interactions are different. Every sequence on LazyGrip includes the talent string it was validated with. If your talents do not match, the sequence is worth importing as a structural reference but treat it as a starting point rather than a finished product. Two runs at your target difficulty with log analysis takes about thirty minutes and tells you everything you need to know about whether it needs tuning for your setup. The validation workflow used for every sequence published on LazyGrip is: run the content, export the Warcraft Logs CSV, check the key metrics, adjust one thing if a metric is out of range, rerun. No sequence gets published before at least two validated runs at relevant difficulty. Ready to find a validated sequence to start from? Browse sequences Coming from the legacy program"
  }
]
